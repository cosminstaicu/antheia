# Items used by Eclipse PDT IDE for project management
.settings/
.buildpath
.project
.dbeaver/
# Composer vendor folder
vendor/
# Examples cache folder
examples/cache/
# Folder for private user usage
_private/
# Cache folder content will be ignored inside the repository
examples/cache/*
# The .gitkeep file will be placed inside the cache folder (so the folder is always empty inside the repository)
!examples/cache/.gitkeep
